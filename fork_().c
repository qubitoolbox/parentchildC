#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main()
{
  
  //process ID
  pid_t pid;

    int j;
    pid = fork();
    if ( pid > 0)
    {
      bool start = true;
      
      do
      {
      
        j++;
        
        if(pid > 0)
      
      
      }while(!(start));
    
    
    
    }








}
